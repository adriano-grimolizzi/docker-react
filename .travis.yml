language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t agrimolizzi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true agrimolizzi/docker-react npm run test -- --coverage